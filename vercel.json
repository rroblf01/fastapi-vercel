{
    "builds": [
      {
        "src": "main.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "main.py"
      }
    ],
    "buildCommand": "pip install -r requirements.txt",
    "devCommand": "fastapi run main.py"
  }